import React, {useState} from 'react'

const Button = ({onClick, id, children, isDataShowing}) => {

    const [isClick, setClicked] = useState(false)
    
    return (
        <button
        ></button>
    )
}

export default Button